<tr id='index_menu_<%= index_line.id %>_tr' style='display: none'>
  <td colspan='9' id='index_menu_<%= index_line.id %>_td' class="right">
    <div class="menu">
      <%= links = student_action_links(index_line).join('&nbsp;') %>
    </div>
  </td>
</tr>
<% study_plan = index_line.study_plan %>
<% if @alternate
    @alternate = false
  else
    @alternate = true
  end %>
<tr class='student-line <%= index_line.status_class %> <%= @alternate ? 'alternate' : '' %>' id='index_line_<%= index_line.id %>'>
  <% if @user.has_one_of_roles?(['faculty_secretary', 'dean']) %>
  <td class='index-tags'>
    <%= raw index_tags(index_line) %>
  </td>
  <% end %>
  <th class="printable no-rounded">
   <%= raw student_link(index_line) %>
  </th>
  <td><%= raw index_line.status %></td>
<% if index_line.absolved? %>
  <td><%= raw index_line.disert_theme.defense_passed_on.strftime("%d. %m. %Y") %></td>
<% elsif index_line.interrupted? %>
  <td><%= raw interrupt_to_info(index_line) %></td>
<% elsif study_plan  %>
  <td><%= raw study_plan.status  %></td>
<% else %>
  <td>&nbsp;</td>
<% end %>
  <td><%= raw index_line.year %>.<%= t(:message_0, :scope => [:txt, :view, :students, :_index, :rhtml]) %></td>
  <td><%= raw index_line.study.name %></td>
<% if @user.has_one_of_roles?(['dean', 'faculty_secretary', 'vicerector']) %>
  <td><%= raw index_line.department.short_name %></td>
  <td><%= raw index_line.specialization.code %></td>
<% end %>
<% unless links.empty? %>
  <td class='action-link'>
    <%= raw link_to_show_actions(index_line) %>
    <%= raw link_to_hide_actions(index_line) %>
  </td>
<% end %>
</tr>

<tr id ='index_form_<%= index_line.id %>_tr' class='form-line' style='display: none'>
  <td colspan='9' id ='index_form_<%= index_line.id %>_td' class="right orange">
    <div id ='index_form_<%= index_line.id %>'></div>
  </td>
</tr>
<tr id='index_detail_<%= index_line.id %>_tr'>
</tr>
