<%= student_action_links(index.student) %>
<%= link_to_remote(_("contact"), :url => {:action => 'contact', :id =>
  index.student.id}, :update => "info#{index.id}", :complete =>
  visual_effect(:slide_down, "info#{index.id}")) %>
