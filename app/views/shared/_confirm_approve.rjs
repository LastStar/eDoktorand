document ||= @document
page.hide "approve_form_#{document.id}"
replace ||= 'approvement'
approvement ||= document.approvement
page.replace "index_line_#{document.index.id}",
  student_action_link(document.index)
page.replace_html replace, print_statements(approvement)
