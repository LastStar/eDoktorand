<% for column in Language.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%= @language.send(column.name) %>
</p>
<% end %>

<%= link_to 'Edit', :action => 'edit', :id => @language.id %> |
<%= link_to 'Back', :action => 'list' %>
